
{% for block in section.blocks %}
<div class="heading">{{block.settings.heading}}</div>
<div class="richtex">{{block.settings.description}}</div>
{% endfor %}

{% schema %}
{
  "blocks": [
     {
       "name": "Details",
       "type": "Career",
       "settings": [
         {
           "type": "text",
           "id": "heading",
           "label": "Heading"
         },
        {
 		 "type": "richtext",
  		 "id": "description",
  		"label": "Description"
		}
]
}],

         
  "presets": [
     {
       "name": "Details",
       "settings": {
        },
       "blocks": [
         {
           "type": "Career"
         }
       ]
     }
   ]
     
}
{% endschema %}


  